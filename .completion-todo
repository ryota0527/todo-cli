#!/bin/bash

completion() {
    local cur

    cur="${COMP_WORDS[COMP_CWORD]}"

    COMPREPLY=()

    while IFS= read -r name; do
        COMPREPLY+=("$name")
    done < <(todo comp | grep "^$cur")

}

complete -F completion todo
